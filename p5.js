//data can be anything
// what if I want to store the name?
let name="Shivam";//use " "(Double/single Coat) to store string.
console.log(name,typeof(name));

let age="25";//use " "(Double/single Coat) to store string.
console.log(age,typeof(age));

let unit=20;
console.log(unit,typeof(unit));
